<%= form_for @recipe do |f| %>
  <p>Title:</p>
  <%= f.text_field :title %>
  <p>Ingredients:</p>
  <%= f.fields_for :ingredients do |i| %>
  <%= i.label :name %>
  <%= i.text_field :name %><br>

  <%= i.label :quantity %>
  <%= i.text_field :quantity %><br>
  <% end %>

  <%= f.submit %>
<% end %>
